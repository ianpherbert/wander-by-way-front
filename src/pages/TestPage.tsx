import { Box } from "@mui/material";
import Map from "../features/map/Map";


export default function TestPage() {
    return (
        <Box >
            <Map height={800} width={800}/>
        </Box>

    )
}